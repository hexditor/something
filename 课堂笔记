保留小数的方法:
#include <iomanip>
using namespace std:
int main()
{

double k=0.123456;
//变量必须是浮点型
//前三种作用于全局，最后一种只作用于一个变量
cout<<setiosflags(ios::fixed) <<setprecision(2)<<endl; 

cout.setf(ios::fixed); 
cout<<setprecision(2)<<endl;

cout<<fixed<<setprecision (2)<<endl;

printf("%.3f",k);
}
